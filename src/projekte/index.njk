---
title: Projekte
description: Wir präsentieren regelmäßig neue Projekte aus allen Bereichen unseres Leistunsspektrums. 
layout: layouts/default.njk

eleventyNavigation:
  key: Projekte

---

<main class="px-4 mx-auto mt-12 mb-28 max-w-default" x-data="{ filter: false }">
  <h1 class="text-4xl font-extrabold tracking-tight">{{title}}</h1>
  <p class="mt-6 text-lg max-w-prose">{{description}}</p>

  <div class="sticky top-0 flex flex-wrap items-center justify-start py-2 mt-16 space-x-2 md:space-x-4 lg:flex-nowrap">
    <p class="mb-2 text-base font-semibold">Filter:</p>

    {% set projektTags =collections | keys | sort | except('all','projekt','post','posts') %}
    {% for item in projektTags %}
    <button class="px-2 py-1 mb-2 text-xs font-semibold transition-all border rounded-md border-amber-300"  x-on:click="filter = '{{item}}'; console.log('Aktueller Filter: '+filter)" :class="filter=='{{item}}' ? 'bg-amber-500' : 'bg-amber-300'" >
      {{item}}
    </button>
    {% endfor %}

    <button class="px-2 py-1 mb-2 text-xs font-semibold border rounded-md border-amber-300" x-on:click="filter = false; console.log('Aktueller Filter: '+filter)">Filter entfernen</button>
  </div>
    
  <div class="grid grid-cols-1 gap-6 mt-8 md:grid-cols-3 md:gap-12 ">
  {% for item in collections.projekt %}
    <article class="relative hover:drop-shadow-md hover:-translate-y-0.5 transition-all hover:cursor-pointer pb-20 overflow-hidden bg-white border-2 rounded-lg group hover:border-amber-300" x-show="'{{item.data.tags}}'.includes(filter)||filter==false" >
      <a href="{{item.url}}">
      <div class="aspect-w-3 aspect-h-2">
      {% imageNewsGallery item.data.heroImage, "w-full h-full object-cover", item.data.title, "(min-width: 768px) 320px, 100vw" %}         
      </div>
      <div class="px-4 mt-6">
        <!-- project Tags -->
        <div class="flex flex-wrap">
         {% for item in item.data.tags | sort | except("projekt") %}
          <span class="p-1 mb-2 mr-2 text-[11px] font-medium leading-none tracking-wider uppercase rounded-full text-amber-700 bg-amber-100">{{item}}</span>
          {% endfor %}
        </div>
        <h2 class="text-xl font-bold md:text-2xl ">{{ item.data.title }}</h2>
        <p class="mt-2 text-sm text-gray-600 md:text-base">{{item.data.page.excerpt}}</p>
        <p class="absolute px-2 py-1 border rounded-lg border-amber-400 bottom-4 group-hover:underline">Projekt ansehen »</p>     
      </a>
    </article>
  {% endfor %}
     </div> 

</main>

