<section id="team" class="py-20">
  <header class="px-4 mx-auto">
    <h2 class="text-2xl font-extrabold md:text-3xl lg:text-4xl md:text-center">Unser Team</h2>
    <p class="mx-auto mt-2 text-lg text-gray-600 md:text-center max-w-prose">Wir stellen uns vor! Unsere festangestellten Mitarbeiter sind in verschiedenen Bereichen spezialisiert.</p>
  </header>

  <div class="px-4 mx-auto mt-12 max-w-default">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-6 lg:grid-cols-3 lg:gap-y-12 lg:gap-x-8">

      {% for person in team %}
        <div class="flex items-center justify-start">
          <div class="w-24 h-24 overflow-hidden rounded-md">
            {% img person.image, "object-cover w-full h-full", person.name, "96px" %}
          </div>
          <div class="ml-4 md:ml-6">
            <p class="text-xl font-bold">{{person.name}}</p>
            {% if person.education %}
              <p class="mt-2 font-semibold">{{person.education}}</p>
            {% endif %}
            {% if person.position %}
              <p class="text-gray-600">{{person.position}}</p>
            {% endif %}
          </div>
        </div>
      {% endfor %}

    </div>
  </div>

</section>